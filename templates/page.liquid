<div class="section">
  <div class="container">
    <div class="page">
      <h1 class="page__title">{{ page.title }}</h1>
      
      {%- if page.content != blank -%}
        <div class="page__content">
          {{ page.content }}
        </div>
      {%- endif -%}
    </div>
  </div>
</div>
